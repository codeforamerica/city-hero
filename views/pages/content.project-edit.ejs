<!-- -->
<div class="project_description">
    <input type="hidden" name="_id" value="<%= project._id %>" />
    <input type="hidden" name="slug" value="<%= (project.slug) ? project.slug : '' %>" />
    
    <div class="project_image_box">
        <% if (project._attachments && project._attachments['project-image']) { %>
        <img class="project_image" src="http://<%= auth.db.host %>:<%= auth.db.port %>/projects/<%= project._id %>/project-image" />
        <% } else { %>
        <div class="project_image-wizard-tip in-place-wizard-tip">
            <p>A picture is much more likely to draw interest from potential supporters than a text-only project page, and a video is even more effective.</p>
            
            <p>Think about what image would represent your project well.  You can use a service like <a href="http://pixlr.com/">Pixlr.com</a> or <a href="http://www.aviary.com/">Aviary.com</a> to edit that image. Your picture should be about 600 by 400 pixels in size and less than 100kb.</p>
            <div class="field project_image_field">
                <div class="label-wrapper">
                    <label for="project_image_field">Image</label>
                    <!--span class="form-item-help">What you are going to do</span-->
                </div>
                <input type="file" class="textfield" name="project_image" />
            </div>
            
            <p>If video is your thing then try to get your message to be no more than 30 seconds long.  Upload it to <a href="http://www.youtube.com/">YouTube</a> and paste the link below.</p>
            <div class="field project_video_field">
                <div class="label-wrapper">
                    <label for="project_video_field">Video</label>
                    <!--span class="form-item-help">What you are going to do</span-->
                </div>
                <input type="text" class="textfield" name="project_video" id="project_video_field" />
            </div>
        </div>
        <% } %>
    </div>
                
    <div class="social_bar">
        <div id="fb-root"></div><script src="http://connect.facebook.net/en_US/all.js#xfbml=1"></script><fb:like href="" send="false" width="450" show_faces="true" font=""></fb:like>
    </div>
    
    <div class="project_text">
        <div class="project_overview">
            <header>
                <h1>About This Project</h1>
            </header>
            <p><textarea name="description" id="description" class="project-edit-form"><%= project.description %></textarea></p>
        </div>
        
    </div>
</div>
<div class="button-floater">
    <ul class="edit-controls">
        <li><input type="submit" value="save your project" /></li>
        <li><a href="/projects/<%= (project.slug) ? project.slug : project._id %>">Finished editing</a></li>
    </ul>
</div>

