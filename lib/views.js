/**
 * @fileoverview Controllers for projects.
 */

var controllers = require('./controllers');

HomePage = {
    getView : function() {
    }
}

ProjectWizard = {
    getView : function() {
    }
};
