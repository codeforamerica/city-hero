/**
 * Wayfinder controller.  This should extend object controllers,
 * like users.
 *
 * DO LATER!!!
 */
exports.wayfinder = function() {
    this.currentObject = this.currentObject || {};
    this.steps = this.steps || [];
}

/**
 * Combine current object with steps
 */
exports.wayfinder.findWay: function() {
    if (this.steps) {
    
    }
};